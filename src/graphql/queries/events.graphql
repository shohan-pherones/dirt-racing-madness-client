query Events {
  events {
    id
    name
    description
    imageUrl
    dateTime
    location
    capacity
    status
  }
}

query Event($id: String!) {
  event(id: $id) {
    id
    name
    description
    imageUrl
    dateTime
    location
    capacity
    status
  }
}
